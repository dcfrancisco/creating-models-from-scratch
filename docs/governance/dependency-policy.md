# Dependency Policy

- Minimize dependencies.
- Document each dependency purpose, license, compatibility, and fallback.
- Do not use dependencies that hide concepts currently being learned.

Current dependencies:
- NumPy (required): numerical baseline
- PyTorch (optional): autograd/tensors only after compatibility evidence
- pytest/ruff/mypy (dev-only)
