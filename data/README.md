# Data directory

This directory stores dataset inputs used by the project.

- Keep large datasets out of Git.
- Keep only tiny fixtures in `tests/fixtures/`.
- Document dataset provenance and licensing in `docs/governance/dataset-card.md`.
