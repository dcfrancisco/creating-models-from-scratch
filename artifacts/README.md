# Artifacts directory

Runtime outputs (checkpoints, logs, experiment records) are written under this directory.

Expected runtime layout (ignored by Git):

- `artifacts/checkpoints/`
- `artifacts/logs/`
- `artifacts/runs/`
